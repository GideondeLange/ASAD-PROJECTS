{
  "name": "asad-projects",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/autorotate.js && node scripts/imgdims.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "vite": "^8.1.0",
    "vite-plugin-image-optimizer": "^2.0.3"
  }
}
